import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'friend-finder-ar',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
